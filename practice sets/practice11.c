#include <stdio.h>

int main()
{
    // natural numbers from 10 to 20 when i =0
    int i = 0;
    while (i < 21)
    {
        if (i >= 10)
        {
            printf("%d \n", i);
        }
        i++;
    }

    return 0;
}